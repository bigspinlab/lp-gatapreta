{
    "name": "lp-gatapreta-parceljs",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "clean": "rimraf dist",
        "start": "parcel src/index.html",
        "build": "npm run clean && parcel build src/index.html --no-source-maps --public-url ./"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.20",
        "parcel": "^2.12.0",
        "postcss": "^8.4.47",
        "rimraf": "^5.0.10",
        "tailwindcss": "^3.4.13"
    }
}
